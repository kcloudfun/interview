# 技术设计历程

## 2024.11.5
本来想实现效果是，启动时候自动扫描全项目代码，自动加载A.java里面内容，生成好面试题的标题以及文字描述、演示代码等。

但实际面临两个困难点，一是怎么灵活获取到A.java内容，保证既不和Java注释风格没有很大冲突（这样直接阅读代码也不影响），
又能规范性读取？
二是演示代码涉及多个文件要怎么展示效果更好，最好是能像idea一样点击跳转（超链接？）。

第二个问题实际还是要取决于先解决第一个问题，第一个问题常见的Java手段诸如反射、切面、类加载器似乎都不怎么行，
好像只有二进制流解析或者反编译手段，都相对实现比较困难，预计都得半月到一个月左右。

暂时先搁置，留到后面有空了去处理。